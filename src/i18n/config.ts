import type { Locale } from '@/data/types';

export const languages: Locale[] = ['cs', 'en'];
export const defaultLanguage: Locale = 'cs';

export const labels: Record<Locale, string> = {
  cs: 'Česky',
  en: 'English',
};

